{
  "name": "astrorock-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-assets": "bun sync-assets.ts",
    "dev": "bun run sync-assets && vite",
    "build": "bun run sync-assets && vite build",
    "preview": "vite preview",
    "wasm": "wasm-pack build ../astrorock-wasm --target web --out-dir ../demo/public/pkg --no-typescript"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
